This directory contains a single file JSON compilation of the words from the
madlibs repository. The original library uses code that cannot be bundled with
browserify. The library is no longer maintained so we extract the needed word
lists here.

https://github.com/stereosteve/madlibs
